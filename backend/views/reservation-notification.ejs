<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #6F4E34; color: white; padding: 10px; text-align: center; }
    .content { padding: 20px; background-color: #f9f9f9; }
    .footer { margin-top: 20px; font-size: 12px; color: #777; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>
        <% if (notificationType === 'new') { %>
          New Reservation Notification
        <% } else { %>
          Reservation Status Update
        <% } %>
      </h1>
    </div>
    
    <div class="content">
      <p>Hello <%= admin.name %>,</p>
      
      <% if (notificationType === 'new') { %>
        <p>A new reservation has been created:</p>
      <% } else { %>
        <p>A reservation status has been updated:</p>
      <% } %>
      
      <h3>Reservation Details:</h3>
      <ul>
        <li><strong>ID:</strong> <%= reservation._id %></li>
        <li><strong>Name:</strong> <%= reservation.userName %></li>
        <li><strong>Date:</strong> <%= new Date(reservation.date).toLocaleDateString() %></li>
        <li><strong>Time:</strong> <%= reservation.timeSlot %></li>
        <% if (notificationType === 'status') { %>
          <li><strong>Status:</strong> <%= reservation.status %></li>
        <% } %>
      </ul>
      
      <p>You can view this reservation in your admin dashboard.</p>
    </div>
    
    <div class="footer">
      <p>This is an automated message. Please do not reply directly to this email.</p>
    </div>
  </div>
</body>
</html>